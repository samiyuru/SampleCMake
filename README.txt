Run the following commands after changing the current
directory to the project root.

To generate a makefile for the current platform run:
    cmake .

To build the project using the generated makefile run:
    make

To clean the project run:
    make clean

To run the program run:
    ./main
